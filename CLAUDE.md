# كنكنة — دليل المشروع لـ Claude

## معلومات أساسية
- **اسم المشروع:** كنكنة (الاسم المعروض بكل مكان — الأيقونة، عنوان النافذة/التاب، Android label). اسم حزمة Dart والمجلد ومعرّف حزمة أندرويد بقوا `kinkan_calc` / `com.example.kinkan_calc` بدون تغيير عمداً (تغييرهم أعمق وأخطر: يفقد المستخدم بياناته المحفوظة على أندرويد لو تغيّر الـ applicationId)
- **المسار:** `C:\Users\altur\kinkan_calc`
- **اللغة:** Flutter / Dart
- **المنصات المستهدفة:** Android + Windows
- **صاحب المشروع:** Muhannad Alturki

## قواعد العمل
- بعد كل تعديل على الكود، سوّ `git commit` بوصف **عربي** يعكس التغيير
- الكود كله في ملف واحد: `lib/main.dart`
- **إذا سأل المستخدم سؤالاً، جاوب فقط ولا تنفّذ أي شيء** — نفّذ فقط عند الأمر الصريح
- **ترقيم النسخة (`kAppVersion` في `main.dart` + `version` في `pubspec.yaml`):** `major.minor.patch` (Semantic Versioning) — رقم الـ patch (الأخير) يُرفع مع أي إصلاح بسيط/خطأ، رقم الـ minor (الأوسط) يُرفع مع دفعة ميزات جديدة، رقم الـ major (الأول) للتغييرات الجذرية. رقم البناء بعد `+` بـ pubspec يُرفع بكل مرة أيضاً (لا يتكرر)

### بيئة تجريبية (Staging) قبل الإنتاج — إلزامي من الآن
- **لا تدفع على `master` مباشرة لأي تعديل بالكود.** ادفع فقط على فرع الجلسة الحالي (فرع التطوير)، اللي ينشر تلقائياً على رابط تجريبي منفصل: **`https://muhannadalturki.github.io/kinkan_calc/beta/`**
- الرابط التجريبي يظهر فيه شريط أصفر أعلى الشاشة "🧪 نسخة تجريبية للاختبار — مو الإنتاج" (عبر `kIsStaging` في `main.dart`، مبني على `--dart-define=IS_STAGING=true` بـ`deploy-web-staging.yml`) — يميّزه بصرياً عن الإنتاج تلقائياً، ما يحتاج أي تفريق يدوي بالكود
- **ما يوصل رابط الإنتاج (`kinkan_calc/` بدون `/beta/`) ولا الـ APK إلا بعد ما يجرّب المستخدم على `/beta/` ويأكّد صراحة** — عندها فقط يُدفع **نفس الـ commit** (بدون أي تعديل إضافي) على `master`
- بما إن نفس الـ commit يُدفع على master بدون تغيير، رقم النسخة يبقى متطابق تلقائياً بين التجريبي والإنتاج — ما يحتاج ترقيم منفصل للتجريبي
- workflow التجريبي: `.github/workflows/deploy-web-staging.yml` (يُشغَّل عند push لفرع الجلسة، ينشر بمسار `beta/` بنفس موقع GitHub Pages). workflow الإنتاج (`deploy-web.yml`) فيه `keep_files: true` حتى ما يمسح مجلد `/beta` عند نشر الإنتاج، والعكس صحيح

## البنية والكلاسات الرئيسية

### كلاسات البيانات
- **`Player`** — لاعب له `name`, `score`, `out` (خرج من اللعبة)
- **`Round`** — جولة أو حدث. `isEvent` = true لو كانت إضافة/حذف لاعب وليست جولة حقيقية
- **`GameRecord`** — سجل لعبة كاملة: `players`, `limit`, `rounds`, `finalScores`, `durationSeconds`
- **`_RolesData`** — يحتوي `silent` (الساكتون) و`winners` (الفائزون) لجولة واحدة

### كلاسات التخزين
- **`SavedGames`** — حفظ وتحميل الألعاب من `SharedPreferences`
- **`SavedNames`** — أسماء اللاعبين المحفوظة للاقتراح
- **`ThemeManager`** — حفظ الوضع الداكن/الفاتح
- **`StatsManager`** — تاريخ إعادة تعيين الإحصائيات

### الشاشات
- **`PlayerSetupScreen`** — شاشة البداية لإدخال أسماء اللاعبين وحد النقاط
- **`GameScreen`** — شاشة اللعبة الرئيسية (الأطول والأهم)
- **`RoundsHistoryScreen`** — سجل جولات اللعبة الحالية
- **`HistoryScreen`** — سجل جميع الألعاب السابقة
- **`TopWinnersScreen`** — شاشة الإحصائيات والترتيب
- **`GameDetailScreen`** — تفاصيل لعبة محفوظة

### Widgets مهمة
- **`_EditPlayersSheet`** — ورقة تعديل اللاعبين: تدعم إضافة، حذف، وإعادة ترتيب بالسحب والإفلات
- **`_GameSettingsSheet`** — ورقة إعدادات اللعبة (حد النقاط، النوع)
- **`_PlayerEntry`** — يوحّد اللاعبين الموجودين والجدد في ورقة التعديل (`originalName == null` يعني لاعب جديد)
- **`_PlayerAdditionCard`** — بطاقة حمراء/خضراء تظهر في السجل عند إضافة لاعب
- **`_PlayerRemovalCard`** — بطاقة حمراء تظهر في السجل عند حذف لاعب
- **`PlayerAvatar`** — أفاتار اللاعب بلون مخصص حسب ترتيبه

## منطق مهم

### الساكتون والفائزون
- دالة `_sortByCircularOrder` تعتمد على ترتيب `widget.players` لتحديد الساكتين
- ترتيب اللاعبين في `widget.players` **يؤثر على النتيجة** — لذلك إعادة الترتيب في `_EditPlayersSheet` مهمة
- `_applyPlayerEdits(renames, newPlayers, finalOrder)` تطبّق التعديلات وتُعيد ترتيب `widget.players`

### نقطة بداية اللاعب الجديد
```dart
final startingScore = maxScore > 0 ? maxScore + 1 : 0;
```
لو كل اللاعبين عند 0، اللاعب الجديد يبدأ من 0 أيضاً.

### قاعدة نهاية اللعبة وعدد الفائزين (v1.1.0)
- `winnersCount` إعداد عام (افتراضي 2)، يُلتقط وقت بدء اللعبة ويُخزَّن مع المسودة (`DraftGame`) والسجل (`GameRecord`)
- التقييم في `_evaluateEnd` بعد كل جولة:
  - آمنون (تحت الحد) أكثر من `winnersCount` → اللعبة مستمرة والمتجاوزون خاسرون
  - آمنون بعددٍ من 1 إلى `winnersCount` → تنتهي والآمنون هم الفائزون فقط (ولو أقل من العدد المحدد — لا يُكمَّل مقعد من المتجاوزين أبداً)
  - لا آمن إطلاقاً (الكل تجاوز بنفس الجولة) → تُملأ المقاعد من المتجاوزين بمجموعات متساوية النقاط تصاعدياً؛ ملء تام/جزئي يُنهي اللعبة، وأول مجموعة أكبر من المقاعد = يستمر الجميع باللعب حتى تتفرق النقاط
- الفائزون الفعليون يُخزَّنون في `GameRecord` (حقل `winners`)؛ الألعاب القديمة بدون الحقل تستنتجهم من النقاط (getter fallback)
- `completed = false` عند الخروج منتصف اللعبة → تُستثنى من الإحصائيات (`_loadStats` يتخطاها) وتظهر "لعبة غير مكتملة" بالسجل والتفاصيل
- شاشة الإعداد تمنع بدء لعبة عدد لاعبيها ≤ `winnersCount`

### الأزرار السريعة وتفعيل الأنواع (v1.1.0)
- زرّان سريعان لكل نوع (`khalasQuick1/2`، `dablQuick1/2`، `dablLonQuick1/2`) يظهران فقط بعد اختيار نوع الجولة — حلّا محل زرّي "شرهة" و"+25" القديمين
- كل نوع لعب له مفتاح تفعيل (`khalasEnabled`...) — المعطّل يختفي من شاشة اللعب وبطاقة إعداداته، ويجب بقاء نوع واحد مفعّلاً على الأقل
- رقم النسخة `kAppVersion` ثابت فوق `SettingsScreen` في `main.dart` ويظهر أسفل شاشة الإعدادات — يُرفع مع كل دفعة تعديلات بالتزامن مع `version` في pubspec

### الجولات مقابل الأحداث
- `r.isEvent` = true → إضافة أو حذف لاعب (لا تُحسب في عدد الجولات ولا في المجاميع)
- `_realRoundsCount` يستخدم `!r.isEvent`
- `_recalcTotals` يتخطى الأحداث بـ `if (r.isEvent) continue`

### حذف لاعب
- اللاعبون في `widget.players` غير الموجودين في `finalOrder` يُعتبرون محذوفين
- يُسجَّل الحذف كـ `Round(isPlayerRemoval: true, removedPlayers: [...])`

## GitHub Actions (بناء APK تلقائياً)
- الملف: `.github/workflows/build-apk.yml`
- يُشغَّل تلقائياً عند كل `push` على `master`
- ينتج APK في Artifacts باسم `kinkan-calc-apk`
- Flutter version: `3.32.4` (مطابق للنسخة المحلية)
- للتنزيل: `https://github.com/MuhannadAlturki/kinkan_calc/actions`

## الحزم المستخدمة
```yaml
confetti: ^0.7.0          # احتفال عند الفوز
fl_chart: ^0.68.0         # الرسم البياني في الإحصائيات
share_plus: ^9.0.0        # مشاركة نتائج الألعاب
google_fonts: ^6.2.1      # خط Cairo
shared_preferences: ^2.2.2 # حفظ البيانات محلياً
uuid: ^4.3.3              # معرّف فريد لكل لعبة
flutter_localizations     # دعم اللغة العربية
```

## ما تم إنجازه
- [x] إعادة ترتيب اللاعبين بالسحب والإفلات في ورقة التعديل
- [x] حذف لاعب من اللعبة مع تسجيله في السجل
- [x] تسجيل إضافة/حذف اللاعبين في سجل اللعبة
- [x] نقطة بداية صحيحة للاعب الجديد (0 لو الجميع عند 0)
- [x] إزالة أزرار الحذف الجماعي (الإحصائيات والسجل)
- [x] متوسط النقاط لكل لاعب في الإحصائيات (مع استبعاد النقاط الشاذة)
- [x] أطول سلسلة فوز لكل لاعب في الإحصائيات
- [x] GitHub Actions لبناء APK تلقائياً
- [x] إعداد "عدد الفائزين" + قاعدة نهاية اللعبة مع حالات التعادل (v1.1.0)
- [x] تخزين الفائزين الفعليين وعلامة اكتمال اللعبة في السجل (v1.1.0)
- [x] استثناء الألعاب غير المكتملة من الإحصائيات (v1.1.0)
- [x] زرّان سريعان قابلان للتعديل لكل نوع لعب بدل الشرهة و+25 (v1.1.0)
- [x] تفعيل/تعطيل أنواع اللعب من الإعدادات (v1.1.0)
- [x] إصلاح إعادة الرسم عند فتح شاشة اللعب (استكمال لعبة محفوظة) (v1.1.0)
- [x] تسميات شارة المتصدرين وحد أقصى للعرض بعدد الفائزين (v1.1.0)
- [x] عرض رقم النسخة في الإعدادات (v1.1.0)
- [x] خروج المجموعات الأعلى نهائياً عند تعادل يمنع فصل الفائزين + إعادة حساب النقاط من السجل عند الرجوع من الخلفية (v1.1.1)
- [x] إجبار إعادة بناء شجرة شاشة اللعب بالكامل (`_redrawKey`) عند الرجوع من الخلفية، تقوية لإصلاح تصفير النقاط (v1.1.2)
- [x] تعطيل PWA service worker الافتراضي لتطبيق الويب (`--pwa-strategy=none`) — كان يخزّن نسخة قديمة من التطبيق بجهاز المستخدم ويرفض تحديثها إلا بإغلاق كامل، فيتكرر ظهور أخطاء مُصلَحة فعلياً على السيرفر وما يتحدث رقم النسخة المعروض (v1.1.3)
- [x] السبب الحقيقي لتصفير النقاط عند الرجوع من الخلفية: `_recalcTotals` كانت تصفّر نقاط `widget.players` فوراً ثم تعيد جمعها من `_history`؛ لو صار استثناء غير متوقع أثناء إعادة الحساب (لوحظ فعلياً عبر تسجيل شاشة) تبقى النقاط مصفّرة نهائياً لأن الحساب توقف بمنتصفه. الإصلاح: `_recalcTotals` تحسب الآن على نسخ مؤقتة (`Player.clone()`) ولا تطبّق النتيجة على الحالة الحقيقية إلا بعد نجاح إعادة تشغيل السجل بالكامل داخل try/catch — فشل الحساب يحافظ على آخر نقاط صحيحة بدل تصفيرها (v1.1.4)
- [x] **إصلاح v1.1.4 ما كفى** — المشكلة استمرت رغم الحماية بـtry/catch، يعني التشخيص (استثناء يوقف الحساب بمنتصفه) غلط أو غير كامل. أضيف تشخيص مؤقت مرئي بأعلى شاشة اللعب (`(سجل:N أخطاء:M)` جنب رقم الجولة) يعرض `_history.length` الفعلي لحظة الرسم وعدد مرات فشل `_recalcTotals` الحقيقي، + SnackBar يظهر نص الاستثناء الفعلي لو صار أي فشل حقيقي. الهدف: نلتقط دليل ملموس بالمرة القادمة اللي تصير فيها المشكلة بدل تخمين حل سادس بدون دليل (v1.1.5 — بناء تشخيصي، يُزال لاحقاً بعد إيجاد السبب الجذري)
- [x] **دليل قاطع من تشخيص v1.1.5:** المستخدم أعاد السيناريو وأرسل صورة تُظهر `(سجل:3 أخطاء:0)` — يعني `_history` فعلاً سليم (3 جولات حقيقية) و`_recalcTotals` لم تفشل بأي استثناء إطلاقاً، ومع ذلك كل النقاط طلعت صفر. هذا **يبطل تماماً** نظرية "استثناء يقطع الحساب" (v1.1.4) — المشكلة بمكان آخر بمنطق الجمع أو التطبيق نفسه رغم نجاحه الظاهري. أضيف تشخيص أدق (v1.1.6): `جمع:X` (مجموع نقاط working فور نجاح حلقة الجمع، قبل تطبيقها) و`تطبيق:Y` (مجموع widget.players الفعلي بعد التطبيق) بجانب نفس السطر، + سجل آخر 6 أحداث دورة حياة التطبيق (`resumed`/`paused`/...) بسطر منفصل تحت رقم الجولة. لو X=0 فالعلة بحلقة الجمع نفسها رغم عدم رميها استثناء؛ لو X صحيح وY=0 فالعلة بخطوة نسخ النتيجة لـwidget.players تحديداً
- [x] **مفاجأة من تشخيص v1.1.6:** صورة أخرى من المستخدم أظهرت `جمع:1500 تطبيق:1500` (متطابقين وصحيحين، مب صفر!) بينما بطاقات اللاعبين تحتها تعرض صفر للجميع. يعني `_recalcTotals` نجحت فعلاً وطبّقت نتيجة صحيحة على `widget.players`، لكن العرض يخالفها. هذا يرجّح إما تصفير لاحق بمسار مختلف تماماً عن `_recalcTotals`، أو خلل عرض حقيقي. أضيف تشخيص فاصل (v1.1.7): سطر يعرض قراءة حيّة فورية لكل اسم:نقطة من `widget.players` وهوية القائمة (`identityHashCode`) وقت الرسم مقابل وقت آخر تطبيق ناجح — لو الهويتان مختلفتان فالقائمة استُبدلت بأخرى؛ لو متطابقتين والقراءة الحيّة نفسها صفر فالمشكلة تصفير فعلي بعد `_recalcTotals` بمسار لم يُكتشف بعد
- [x] **السبب الجذري الفعلي (مؤكَّد بتشخيص v1.1.7):** صورة من المستخدم أظهرت `هوية-رسم` ≠ `هوية-تطبيق` بالضبط — يعني `widget.players` اللي الشاشة تقرأ منه وقت الرسم **كائن مختلف تماماً** عن الكائن اللي `_recalcTotals` حسبت عليه وطبّقت نتيجة صحيحة قبل شوي (بينما `_history` ورقم الجولة والشارات، وكلها حقول State، بقيت سليمة). يعني Flutter (على الأرجح ضمن آلية إعادة بناء الـ Route/ModalRoute على الويب عند الرجوع من الخلفية) يستبدل كائن `widget.players` بنسخة جديدة بنقاط افتراضية صفرية، مع محافظته على نفس كائن الـ State. **الإصلاح الجذري (v1.1.8):** أضيف حقل `_players` مملوك بالكامل من الـ State (`late List<Player> _players`)، يُهيّأ مرة وحدة من `widget.players` بـ`initState` (`_players = List.of(widget.players)`)، واستُبدلت كل استخدامات `widget.players` داخل `_GameScreenState` بـ`_players` — فبيانات اللاعبين صارت تعيش بالكامل بالـ State المُثبَت أنه لا يتأثر بهذا الاستبدال، بدل الاعتماد على `widget.players` غير الموثوق. التشخيصات المرئية (سجل/أخطاء/جمع/تطبيق/دورة الحياة/القراءة الحيّة) أُبقيت مؤقتاً للتأكد من نجاح الإصلاح (لازم تتطابق الهويتان دائماً الآن)، وتُزال لاحقاً بعد التأكيد
- [x] **تأكيد نجاح الإصلاح الجذري + تنظيف (v1.1.9):** المستخدم أكّد بصورة إن الهويتين متطابقتين دائماً الآن والنقاط صحيحة — حُذفت كل الأسطر والحقول التشخيصية المؤقتة (`_recalcErrorCount`, `_lastWorkingSum`, `_lastAppliedSum`, `_lifecycleLog`, `_lastAppliedListIdentity`). بالتزامن، تسمية شارة "الساكتين" تغيّرت إلى **"🃏 الموزّعون"** بلون أحمر (`AppColors.danger`) — الاسم الأدق لدورهم الفعلي بجمع الورق وتوزيعه (السكوت كان مجرد عادة مصاحبة لا يصف الدور)، وتسميتا شارتي المتصدرين تغيّرتا من "المتصدرون الآن"/"يليهم" إلى **"الأول"/"الثاني"** لتوضيح أوضح
- [x] **بيئة تجريبية (Staging) منفصلة عن الإنتاج (v1.1.10):** بعد سلسلة التشخيصات الطويلة على رابط الإنتاج مباشرة، أضيف `deploy-web-staging.yml` ينشر تلقائياً عند push لفرع الجلسة على `kinkan_calc/beta/` (نفس موقع GitHub Pages، مسار فرعي منفصل)، مع شريط أصفر تلقائي "🧪 نسخة تجريبية" (`kIsStaging` في `main.dart`، مبني على `--dart-define=IS_STAGING=true`). القاعدة الجديدة: كل تعديل يُدفع لفرع الجلسة فقط أولاً، ولا يوصل `master`/الإنتاج ولا الـ APK إلا بعد تأكيد صريح من المستخدم على `/beta/` — بنفس الـ commit بالضبط فيبقى ترقيم النسخة متطابق بين البيئتين تلقائياً
- [x] **تعديلات تجريبية بدون ترقيم (على 1.1.11):** إعادة ترتيب شارات منتصف اللعبة إلى (الأول، الثاني، 🃏 الموزّعون) بما إن الموزّعون دائماً أطول قائمة، والفاصل بين الأسماء داخل كل شارة تغيّر من `، ` إلى ` | ` لوضوح أكبر — بطلب صريح من المستخدم بالبقاء على التجريبية بدون رفع رقم النسخة لأنها تعديلات تجميلية بسيطة
- [x] **حذف أيقونة الميوت من شارة الموزّعون + عكس سهم سجل الجولات + إعداد نقاط اللاعب المضاف + إصلاح دخوله المبكر للشارات (v1.1.12):** شارة 🃏 الموزّعون صار بدون أيقونة (icon اختياري الآن بـ`_buildRankChip`)، والسهم بين "قبل" و"بعد" بكرت الجولة بـ`_RoundDetailCard` انعكس اتجاهه (`arrow_forward_rounded` بدل `arrow_back_rounded`) ليتماشى مع اتجاه القراءة. أُضيف إعداد عام جديد **"نقاط اللاعب المضاف لاحقًا"** (`newPlayerScoreOffset`، افتراضي 1) بشاشة الإعدادات مع شرح واضح تحت الحقل يوضح أثره (نقطة بداية اللاعب المضاف منتصف اللعبة = أعلى نقطة حالية + هذا الرقم)، بدل الرقم الثابت `+1` بالكود. وأُصلح خلل: شارة "الثاني" (`_secondPlace`) كانت تُحسب من نقاط اللاعبين الحية مباشرة (`_active`) بدل إعادة تشغيل السجل تدريجياً زي "الأول" و"الموزّعون" — فيدخلها اللاعب المضاف فوراً قبل أي جولة. الإصلاح: الثلاث شارات كلها تُحسب الآن ضمن نفس حلقة إعادة تشغيل `_recalcTotals`، مع استثناء أي لاعب أُضيف حديثاً من الثلاث شارات (`pendingNewcomers`) لحد ما تُسجَّل أول جولة حقيقية تشمله
- [x] **إعادة تسمية التطبيق إلى "كنكنة" + أيقونة جديدة (v1.1.13):** الاسم المعروض للمستخدم تغيّر لـ"كنكنة" بكل مكان — عنوان نافذة الويب/التاب (`web/index.html`)، `manifest.json` (name/short_name/description)، تسمية أندرويد (`android:label`)، عنوان نافذة ويندوز (`main.cpp`، بترميز `\u` بدل حرف عربي حرفي بالمصدر تجنّباً لأي التباس بترميز MSVC)، وكل النصوص داخل `main.dart` (عنوان `MaterialApp`، عناوين الشاشات، نصوص المشاركة). اسم حزمة Dart (`kinkan_calc` بـ`pubspec.yaml`) ومعرّف حزمة أندرويد (`com.example.kinkan_calc`) **بقوا بدون تغيير عمداً** — تغييرهم أعمق (يحتاج نقل مجلدات/تعديل كود Kotlin) وأخطر (تغيير الـ applicationId يخلي أندرويد يعتبره تطبيق مختلف تماماً، فيفقد أي مستخدم مثبّت النسخة القديمة كل بياناته المحفوظة). الأيقونة الجديدة (أربع أوراق مروحية بالرموز الحقيقية + اسم "كنكنة" أسفلها) وُلّدت من صورة رفعها المستخدم إلى كل الأحجام المطلوبة (أندرويد mipmap×5، أيقونة ويندوز `.ico` متعددة الأحجام، أيقونات وفافيكون الويب) عبر بايثون/Pillow محلياً بما إن Flutter/`flutter_launcher_icons` غير متوفرين بهذي البيئة؛ نسخ الأيقونة القابلة للقص (`maskable`) بالويب أُضيف لها هامش أمان ٢٢٪ حول الرسمة الأصلية حتى ما يقص القص الدائري أطراف الأوراق أو النص
